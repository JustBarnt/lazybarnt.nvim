;; extends

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "vim"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "NvVim"))

; Operators
[
  "and"
  "not"
  "or"
] @keyword.debug
